const nivelAcceso = 4;
const nivelRequerido = 3;

if (nivelAcceso >= nivelRequerido) {
    console.log("Acceso concedido al área táctica.");
} else {
    console.log("Acceso denegado. Nivel insuficiente.");
}